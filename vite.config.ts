import { defineConfig } from 'vite';

// Served from https://<user>.github.io/tih-tumblr-prs/ — update this if the
// repo is ever renamed or moved to a custom domain / user page.
export default defineConfig({
  base: '/tih-tumblr-prs/',
});
